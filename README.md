# Word Frequency Counter
An application that counts the frequencies of individual words in a text file.

### Installation

```
$ git clone git@github.com:nazwhale/word-frequency-counter.git
$ cd word-frequency-counter
```

### Use
To output the word frequencies for 'The Railway Children' in the command line, simply run:
```
$ node src/frequencyCounter.js
```
